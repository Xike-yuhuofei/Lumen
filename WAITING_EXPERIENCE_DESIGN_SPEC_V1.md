# Waiting Experience Design Spec v1（等待体验设计规范 v1）

> 状态：**FROZEN BASELINE**（T4｜等待体验设计规范，可作为 T5｜系统映射 与后续 UI 实现的输入） · 只定义体验规范并验证 · **不实施生产 UI / 不改代码 / 不进入 T5**
> 冻结标记：`FROZEN_BASELINE = "t4-waiting-experience-design-spec-v1"`（验收见 §15）
> 修正记录（2026-08-20）：T4 语义一致性修正，解决 5 项已确认问题（见 §15.1），修正后**重新冻结**为同一基线
> 日期：2026-08-20 · 输入：冻结的 T1 `PERCEIVED_LATENCY_GAP_AUDIT.md`（G1–G8 / S1–S6）+ **冻结的** T2 `PERCEIVED_WAITING_STATE_MODEL_V1.md` + **冻结的** T3 `PERCEIVED_LATENCY_METRICS_V1.md`
> 依据代码（现状事实）：`frontend/src/app/App.tsx`（`deriveThinkingPhase`/`TOOL_PHASE_LABELS`/`STAGE_PHASE_LABELS`/idle timer/`ask_user` 豁免）、`frontend/src/api/ws.ts`（心跳/重连/resume）、`frontend/src/app/sessions.ts`（错误文案）、`lumen/runtime/session/turn_runtime.py`（事件过滤/终态）
> 关系：**只引用** T1/T2/T3 已冻结语义，不新增语义矛盾、不修改 T2 状态机、不修改 T3 指标定义

---

## 0. 范围与对 T1/T2/T3 的关系（不破坏冻结基线）

本文档回答一个问题：**在某个感知状态 `S/R/T/G/PIN/terminal`、某种等待时长、某个异常场景下，用户应当看到什么级别的反馈、什么粒度的信息、以及什么统一体验原则**。它是 T1（缺口审计）→ T2（状态模型）→ T3（指标契约）在"体验层"的收口，也是 T5 系统映射与后续 UI 实现的**设计基线**。

- **T2 为状态语义唯一来源**：状态集合 `S/R/T/G/PIN/OK/ER/CA/TO`、传输覆盖 `connected/reconnecting/disconnected`、转换表（T2 §2.1）、决定性/文案级白名单（T2 §5/§11.4）、不变量 I1–I11、三平面模型与三条时间轴，全部原样沿用，**本规范不新增任何 P 状态、不修改任何合法边**。
- **T3 为指标边界唯一来源**：M1–M8、`SYS_WAIT` 记账（PIN 用户思考、失联、终态后排除）、场景桶、维度、P50/P95/P99、`CALIBRATION_PENDING` 处理，全部原样沿用；**本规范不修改任何指标定义**。
- **本阶段不实施任何修复**：不实现 UI 改动、不设定已生效的绝对时长阈值（阈值一律标记 `CALIBRATION_PENDING`，见 §3.2）——只交付**可评审、可验收、可度量**的设计规范契约。

**本文档新增的东西（均不破坏 T2/T3）**：
1. 把 T2 的 P 状态与 T3 的指标映射为**可执行的设计约束**：什么反馈元素允许/禁止出现在什么状态（§2/§4–§8）。
2. 定义**等待时长反馈分层**，区分"确定性结构规则"（无需数据即可生效）与"待数据校准规则"（`CALIBRATION_PENDING`，§3）。
3. 定义**维持/升级/降级/终态反馈决策表**（§9）与**禁止模式清单**（§10），并给出**设计评审/UI 验收的 PASS/WARN/FAIL 框架**（§11）。

> **一致性红线**：本文档任何"首反馈/首有效结果/静默窗口/状态切换/终态"的描述，**必须**与 T2 状态机与 T3 指标边界逐一对齐；出现"由计时器伪造推进""把用户思考表现为系统处理"等描述时，即违反红线，一律写入 §10 禁止清单。

---

## 1. 设计基准：感知状态 ↔ 反馈元素（围绕 `S/R/T/G/PIN/terminal`）

### 1.1 反馈元素清单（本规范可调用的 UI 反馈原子）

| 反馈元素 | 定义 | 示例（现状） | 状态依据 |
|---|---|---|---|
| **E1 即时活动指示** | 提交后**同 tick** 出现的、表示"已受理、正在处理"的指示（动画圆点/微光等**非进度型**动效）。**定义**：E1 为本地 UI 即时反馈，在 `submit` 同 tick 出现，**不等同于** T3 M1（TTA，服务端确认延迟）或 M2（TTFD，首决定性事件到达延迟）；E1 承诺"已受理"，不承诺任何后端进度或事件到达 | `setStreaming(true)`+`setStreamPhase('正在处理…')`（App.tsx L3316–3317） | T2 `S` 入口；T1 §3 首反馈 |
| **E2 状态文案** | 描述当前活动态的短语（不含进度数字） | `正在处理…/正在思考…/正在生成回答…` 等 `deriveThinkingPhase` 输出 | T2 §5 文案级事件映射 |
| **E3 阶段/工具指示** | 进入 `R/T` 时对"正在做的事"的简约命名（工具/检索名粒度） | `正在检索资料…/正在运行代码…`（`TOOL_PHASE_LABELS`） | T2 §1.1 `R/T`；T2 §7 暴露到工具粒度 |
| **E4 流式内容** | 首个非空 `content` 起，真实生成的答案内容 | `content` 事件逐 token 渲染 | T2 `G`（首个有价值结果唯一来源） |
| **E5 终态结果** | `OK` 的完成态：停止流式、保留内容、清理活动指示 | `done(completed)` → `setStreaming(false)` | T2 §1.1 `OK` |
| **E6 终态异常块** | `ER/CA/TO` 的非破坏性提示块（保留已见内容，尾部追加） | 超时/失败错误块（`failTurnIdle` 非破坏性追加，App.tsx L3095–3110） | T2 §1.1 `ER/CA/TO`；T1 §11.5 |
| **E7 传输覆盖提示** | 断线/重连/不可达的横幅或指示（**只由传输层真实机制驱动**：心跳超时/重连状态/WS 断开事件，**禁止由应用层静默时长推断** `disconnected`；**不终结** P 状态） | 重连耗尽→红色 `connectError` 横幅（`STREAM_CONNECT_ERROR`） | T2 §1.2 传输覆盖层；I8 |
| **E8 暂停态卡片** | `PIN` 的答题卡片（**无进度指示**，豁免 idle 超时） | `ask_user` 出题卡；`failTurnIdle` 对 question 块直接放行 | T2 §1.1 `PIN`；T2 §8 |

### 1.2 感知状态 ↔ 允许/禁止的反馈元素

> 这是全文的核心对照表：**每个 P 状态可用哪些元素、禁用哪些元素**，均只由 T2 状态语义与 T3 指标边界导出。

| P 状态 | 允许 | 禁止 | 依据 |
|---|---|---|---|
| `S`（等待首个价值/决定性事件） | E1 即时活动指示；E2 状态文案（可由 `thinking/progress/stage_start` 等文案级事件升级，但**不产生状态迁移**） | E3 阶段/工具指示（无决定性 tool_call/sources 不得假装在做什么）；E4 流式内容（无 `content(非空)` 不得出现"回答"占位）；**任何进度条/百分比/伪 ETA** | T2 §1.1 `S`；I2/I3；T2 §7 合并规则 |
| `R`（检索中） | E3 工具/检索名粒度指示（`正在检索/阅读资料…`）；E2 文案；E1 | 子步骤/检索内部细节；**无依据百分比**；子进度条 | T2 §1.1 `R`；T2 §7 子步骤隐藏；T1 G4/S4 |
| `T`（工具执行中） | E3 工具名粒度指示（`正在运行代码…`）；E2 文案；E1 | 工具内部参数、调用轮次、provider/模型名；**子进度百分比** | T2 §1.1 `T`；T2 §7；T1 G4 |
| `G`（生成中=首个有价值结果已到） | E4 流式内容为**主反馈**；E1/E2 降为次要（如"生成中"角标）；**不再显示"处理中"大块文案** | 用状态文案**覆盖/遮蔽**真实内容；在流式内容旁持续显示大号"正在处理…" | T2 §1.1 `G`；本 §4.5；约束"G 中流式内容与状态反馈分离" |
| `PIN`（等用户输入） | E8 暂停态卡片；**无任何"系统仍在处理"指示** | E1/E2/E3/E4 系统处理类指示（用户思考时间不得表现为系统处理）；空闲超时计数 | T2 §1.1 `PIN`；T2 §8 豁免；T3 M6p |
| `OK` | E5 终态结果（停止流式、保留内容、清理指示） | 继续显示活动指示/处理文案 | T2 §1.1 `OK`；终态闩锁 I1 |
| `ER` | E6 终态异常块（非破坏性，保留已见内容） | 把失败伪装成"正在处理"或成功 | T2 §1.1 `ER`；I1 |
| `CA` | E6 取消态（保留已渲染流式件） | 隐藏用户已取消的事实 | T2 §1.1 `CA`；T1 §12.7 不变量 |
| `TO` | E6 超时块（非破坏性；提示可延长等待或重试） | 继续无限等待而无声明 | T2 §1.1 `TO`；T2 §8 |

> **传输覆盖层正交**：`E7` 可与上述任意 P 状态并存（`disconnected` 不是 P 终态，不终结 `S/R/T/G/PIN`，T2 §1.2/§2.4/I8）。

---

## 2. 统一体验原则（设计基线）

> 所有后续条款都是这些原则的展开。原则本身是**确定性结构规则**，不需要数据校准即可评审。

- **P1 事实驱动（输入边界统一）**：一切反馈必须由 **O 平面真实可观测事实**（T2 §0/§11.4）驱动，共五类输入，职责边界无歧义：
  1. **决定性事件**：`content(非空)/tool_call/sources/tool_result(保留 R/T 类属)/wait_for_input/done/error`（T2 §11.4 白名单）——**唯一可推进 P 状态的事件类**；
  2. **文案级事件**：`thinking/progress/stage_start`——只升级当前态文案（E2），**永不推进 P 状态**（T2 §5/§7）；
  3. **传输事实**：`connected/reconnecting/disconnected`（T2 §1.2）——只叠加/解除 E7 传输覆盖提示，**正交于 P 状态**（I8），**永不推进 P 状态**；
  4. **用户本机动作**：`submit`/`submit_user_reply`/`cancel`——决策性动作，分别进入/回到 `S`、进入 `CA`，属可推进 P 状态的事实；
  5. **空闲计时器触发**（合法客户端计时事实）：非 PIN 活动态静默超时 → **仅派发 `TO`**（T2 I3），属可推进 P 状态（至终态）的事实。
  **推进权（唯一）**：只有上述**决定性事实**（决定性事件 ∪ 决策性用户动作 ∪ 空闲计时器）可以推进 P 状态；文案级事件与传输事实只作用于文案/传输覆盖层。禁止：虚假进度、伪阶段、无依据百分比、用动画掩盖系统无进展（T2 I2/I3）；禁止把文案级/传输事实/计时器当作"正在推进"的依据。
- **P2 确定性可回放**：反馈状态机的每个迁移与 T2 §2.1 一致、与 T3 §11 推导算法一致。同一事件日志重放必须得到相同反馈序列（T2 I9）。
- **P3 最小充分粒度**：用户可见颗粒度 = 用户能据此**做决策**的最小充分集（T2 §7）。工具/检索暴露到"工具名"粒度，子步骤与内部细节隐藏。
- **P4 状态文案与内容分离**：`G` 中真实流式内容是最主要反馈；状态文案只作次要角标，**不得覆盖、遮蔽或以任何方式干扰真实内容**（T2 §1.1 `G`）。
- **P5 用户等待 ≠ 系统处理**：`PIN` 用户思考时间不得呈现为系统仍在处理，不得计入系统等待呈现（T3 `SYS_WAIT` 排除 PIN）。
- **P6 异常可解释、可行动**：`ER/CA/TO` 必须给出**一致、非破坏性、可行动**的终态语义（保留已见内容；给重试/延长等待路径），不得静默或伪装。
- **P7 无障碍与非破坏**：活动指示须通过 `aria-live` 等机制对辅助技术可达；任何异常/终态反馈不得删除用户已见内容（T1 §11.5 非破坏原则）。

---

## 3. 等待时长反馈分层（确定规则 vs 待数据校准）

### 3.1 分层的概念框架：极短 / 普通 / 长

等待体验按时长分三档，档位决定"是否需要升级反馈"：

| 档 | 语义 | 边界（时长） | 状态 |
|---|---|---|---|
| **极短** | 用户几乎不感知的等待（如确认 ack） | `M1` 量级 | 无需任何反馈设计（仍须有 E1 同 tick 指示以防闪烁） |
| **普通** | 正常回合的等待（首反馈→首 token 等） | 待校准：以 T3 `M2/M3` 的 **P50–P95** 区间为界 | 维持 E1+E2 文案级反馈即可 |
| **长** | 超过正常分布、用户开始怀疑卡死的等待 | 待校准：以 T3 `M2/M3/M5` 的 **P95/P99** 为界 | 触发 §5 的"长静默处理"与 §11 的验收关注 |

> **核心立场**：档位边界**不得凭空设定**。在 T3 真实采集足够数据前，一切"极短/普通/长"的具体秒数标记为 `CALIBRATION_PENDING`（§3.2）；本规范只给出**分层结构**与**每一档必须满足的反馈要求**。

### 3.2 确定规则（结构性，无需数据校准，立即可评审）

| # | 规则 | 内容 | 依据 |
|---|---|---|---|
| D1 | 同 tick 首反馈（本地即时，非 TTFD） | `submit` 后**同一渲染 tick** 必须出现 E1 即时活动指示（现状已满足，App.tsx L3316–3317）。E1 是**本地即时反馈**（`t_submit` 同 tick），**不是**决定性事件，**不产生/不缩短** T3 M1（TTA）/M2（TTFD）/M3（TTFMR）的测量——这些指标以真实事件到达时刻为终点 | T1 §3 首反馈；T3 §3 M1/M2/M3（起点 `t_submit`） |
| D2 | 静默维持 | `S` 内无任何决定性事件时，E1+E2 必须**持续存在**，不得闪烁消失 | T1 G1；T2 §1.1 `S` |
| D3 | 静默的唯一终结者 | `S` 长静默只能由**决定性事件**（离开 `S`）或 **idle timer → TO**（终态）终结；禁止用计时器伪造推进 | T2 I3（计时器唯一能力是派发 TO）；T3 M8-t |
| D4 | 升级必须真实事件驱动 | 任何 E2→E3（`S→R/T`）或 E2→E4（`S→G`）升级必须由对应的**决定性事件**触发 | T2 I4（活动因果） |
| D5 | 禁止降级文案回退 | 禁止 `result`/空 `content` 等文案级事件把已显示的阶段文案回退为"正在处理…"（T2 §6 映射噪声，本规范直接禁此呈现） | T2 §6/§11.4；T3 §2 |
| D6 | PIN 豁免空闲超时 | `PIN` 期间空闲计时器必须豁免，不得报超时 | T2 §8；T1 §11.5 |
| D7 | 异常非破坏 | `ER/CA/TO` 反馈必须非破坏（保留已见内容、尾部追加），且文案一致 | T1 §11.5；P6 |

### 3.3 待数据校准规则（`CALIBRATION_PENDING`，采集 T3 真实数据后填充）

| # | 校准项 | 内容 | 依赖 |
|---|---|---|---|
| C1 | 档位边界秒数 | "极短/普通/长"的具体时长边界 ← T3 `M2/M3/M5` 分位 | T3 §7 P50/P95/P99 |
| C2 | 长静默升级时机 | 超过 `P95` 后是否升级为"仍在工作/可取消/可重试"提示的**具体阈值秒数** | T3 `M5(P95)` |
| C3 | 超时前"安抚"阈值 | 距 idle 超时还剩多少秒时给出"仍在等待/可以停止"提示的具体秒数 | T3 `M8-t2`（触发前静默段） |
| C4 | 断线可见化时机 | 传输层**已确认 `disconnected` 后**，失联多久再叠加更明确的传输提示（先于 45s 心跳上限）——**仅在真实传输事实上叠加，不在 `S` 静默内凭空显示断线**（§5.2 B7） | T3 `M7a` |

> **约定**：以上均为 `CALIBRATION_PENDING`——现有证据（T1 单点实测 8.256s 静默、S5 退避 5/10/20s）不足以制定稳定绝对阈值，**本阶段不生效为任何已实施的 UI 逻辑或告警**；一旦填充，C1–C4 只调整"升级反馈的时机"，不改变本文档任何确定性规则 D1–D7。
>
> **ETA 准入红线**：任何 P95/P99 等时长校准（含 C1–C4）**只**用于调整"升级/安抚/断线可见化"的时机，**不自动授予 ETA（"预计还需 Xs"）呈现的合法权**——ETA 默认禁止，仅当未来存在**独立且经验证的 ETA 预测契约**时方可另行评审（§10 F3、§5.3）。

---

## 4. 首次反馈、持续反馈、阶段切换、首个有效结果、最终完成

> 对应 T2 §11.2 关键时刻基元（`t_submit/t_first_decisive/t_first_token/t_done`）在体验层的表达。

### 4.1 首次反馈（同 tick E1＝本地即时反馈，禁止混同 TTFD）

- **要求**：`submit` 同 tick 出现 E1 即时活动指示（现状已满足，D1）。
- **边界**：首次反馈只承诺"已受理、正在处理"，**不承诺**任何内容/阶段（无决定性事件前不得显示具体阶段）。
- **与 T3 指标的关系（明确禁止混同）**：
  - **E1** ＝ `t_submit` 同 tick 的**本地即时反馈**，非网络事件、非决定性事件，**不进任何指标口径**；
  - **TTA（M1）** ＝首个 `session` 事件到达 − `t_submit`（服务端确认延迟，T3 §3.1）；
  - **TTFD（M2）** ＝首个**决定性**事件到达 − `t_submit`（首段 `S` 退出点，T3 §3.1）；
  - **TTFMR（M3）** ＝首个非空 `content` 到达 − `t_submit`（首个有价值结果，T3 §3.1）。
  - 因此：**禁止将 E1（本地即时反馈）等同于 First Decisive Feedback（TTFD/M2）**——E1 只是 `t_submit` 处的本地 ack，TTFD/TTFMR 仍以真实到达事件为准，不受 E1 影响。

### 4.2 持续反馈（S 内的维持）

- **要求**：D2 静默维持；文案可由 `thinking/progress/stage_start` 等文案级事件**温和升级**（如"正在思考…"），但**不产生状态迁移、不显示进度**（T2 I2/§7）。
- **禁止**：在 S 内展示阶段/工具/内容占位（见 §1.2 `S` 禁止列）。

### 4.3 阶段切换（S→R/T、S→G 等）

- **要求**：一切阶段切换**只由决定性事件驱动**（D4）。切换时更新 E2/E3 文案；`R/T` 类属由最近决定性 `tool_call` 的 kind 决定（检索→R，非检索→T，T2 §2.2）。
- **禁止**：计时器驱动的伪切换（"等 5 秒就当在思考"）、无意义的阶段轮换（§10 F4）。

### 4.4 首个有效结果（→G）

- **要求**：首个非空 `content` 到达后，UI 立即以 E4 流式内容呈现**真实内容**；之前不得出现"回答占位"。`G` 只由 `content(非空)` 进入（T2 I2）。
- **禁止**：用"正在生成…"动画覆盖首个结果；在 content 之前放"正在思考"大文案（`thinking` 不产生 G）。
- 对应 T3 M3（Time to First Meaningful Result）的体验基线。

### 4.5 G 中流式内容与状态反馈的关系（P4）

- 进入 `G` 后，**流式内容为主反馈**；状态指示（E1/E2）应**降级为次要角标**（如边角"生成中"），不再显示大号"正在处理…"块。
- 状态文案**不得覆盖/遮蔽/滚动干扰**真实内容；内容区不随状态文案跳动。
- 多步 Agent 中从 `G` 再次进入 `R/T` 是真实新阶段（T2 §2.3），此时允许 E3 工具指示短暂出现，但**已生成内容必须保留**，不得清空重来。

### 4.6 最终完成（OK）

- **要求**：`done(completed)` → 停止流式、保留全部已渲染内容、清理活动指示（E5）。终态后事件全部丢弃（T2 I1）。
- **允许**：`OK` 后非阻塞的补充（如标题生成 `session_meta`）**不得干扰**用户已见结果（T1 G7；T2 §5 将 `session_meta` 归为终态后非决定性事件——本规范将其定义为"终态后的可选非阻塞装饰"，不进任何等待呈现）。

---

## 5. `S` 长时间无真实事件时的体验处理（含 Max Silent Gap 对应原则）

> `S` 是 T2 模型里最大的合法静默区（TTFT/摘要/冷启动/静默重试全部并入 `S`，T2 §4）；T3 的 M5 Max Silent Gap 正是对这一静默的测量。本节的体验原则直接回应"长静默窗口/卡死误判"（T1 G1/G2）。

### 5.1 允许的表现

| # | 允许 | 说明 |
|---|---|---|
| A1 | 维持 E1+E2（"正在处理…"）持续存在 | D2；**不闪烁、不消失** |
| A2 | 文案级事件温和升级文案 | `thinking/progress/stage_start` 只增文案、不迁移状态（T2 §7）；`progress` 若后端发出"正在{msg}…"，可呈现 | 
| A3 | idle timer 到点 → TO 终态 | D3：静默的**唯一**兜底终结者；给非破坏超时块（T1 §11.5） |
| A4 | 用户随时可取消 | 停止按钮常驻，`CA` 语义保留已见内容（T2 §8） |

### 5.2 禁止的表现

| # | 禁止 | 违反 |
|---|---|---|
| B1 | 任何进度条 / 百分比 | 无真实依据（T2 I2；约束"无依据百分比"） |
| B2 | 伪 ETA / 剩余时间 | 无数据支撑（§10 F3；ETA 默认禁止，见 §5.3） |
| B3 | 阶段/工具指示（无决定性事件时） | 伪阶段（T2 I4；§10 F2） |
| B4 | 暴露重试次数/退避时长/模型名/provider | 内部技术细节（T2 §7 隐藏；§10 F6） |
| B5 | 用动画节奏暗示"仍在推进"以掩盖无事件 | 动画掩盖无进展（T1 G2；§10 F5） |
| B6 | 静默期结束后假装"刚刚一直在做 X" | 事实不符（P1） |
| B7 | 把应用层静默误判为断线 | 仅凭"连续 N 秒无决定性事件"推断 `disconnected` 并出断线提示——应用层静默属 `S` 长静默（§5.1/§5.3），`disconnected` **只能**由传输层真实机制（心跳超时/重连耗尽）置位（T2 §1.2） |

### 5.3 Max Silent Gap 的体验处理原则（回应完成标准 #4）

1. **度量**：M5 由 T3 按"决定性事件到达间隔，仅限系统等待区间"测量（T3 §3.2），本规范不重复定义。
2. **体验**：M5 的**可感知上限**由 idle timer（默认 180s，可配 30–1800s）兜底——这是唯一承诺"最多等多久"的边界；`S` 内的任何静默段均不得让用户"无限期无承诺"。
3. **升级**：当 `S` 静默超过 T3 分布 P95（`C2`，`CALIBRATION_PENDING`）时，允许升级为"仍在处理中、你可以停止或稍后重试"的**温和提示**——但升级时机阈值**必须**待数据校准，**不得**在此之前凭空显示"已超时"或"即将超时"。
4. **禁止**：不得把 M5 的测量本身变成 UI 逻辑（如"静默 3 秒就转圈更快"）——那是动画掩盖无进展（B5）。
5. **ETA 默认禁止**：任何"预计还需 Xs / 剩余时间"的呈现均默认禁止（§10 F3）。完成 P95/P99 等时长校准（C1–C4）**只**调整升级/安抚/可见化时机，**不自动赋予 ETA 合法权**；仅当未来存在**独立且经过验证的 ETA 预测契约**（如基于真实分位的可验证预测模型）时，方可单独评审准入。
6. **静默 ≠ 断线**：`S` 内长静默属**应用层**合法处理中（TTFT/摘要/冷启动/重试退避，§5.1），**不是**传输故障；断线提示（E7）只能由传输层真实事实（心跳超时/WS 断开/重连耗尽）驱动，禁止用应用层静默时长推断 `disconnected`（§5.2 B7）。

---

## 6. Retrieval / Tool / 多步 Agent 的反馈粒度、阶段切换与信息暴露边界

### 6.1 反馈粒度（用户可理解且有决策价值）

| 场景 | 允许的粒度 | 禁止暴露 | 依据 |
|---|---|---|---|
| 检索 `R` | "正在检索/阅读资料…"（`rag/kb_files/read_source/sources`） | 检索子调用、top-k、分块、embedding 细节 | T2 §1.1 `R`；T1 G4 |
| 工具 `T` | "正在运行代码…/正在获取网页…"（工具名粒度，`TOOL_PHASE_LABELS`） | 工具参数、调用轮次、返回值细节 | T2 §1.1 `T`；T2 §7 |
| 多步 Agent | 简约阶段序列（如"检索→生成→工具→生成"），每步由真实决定性事件切换 | 中间规划、内部状态、每步 token 数 | T2 §2.3（G→R/T 为真实新阶段）；T2 §7 |

### 6.2 阶段切换

- 每次 `R/T` 切换必须由真实 `tool_call`（kind 决定类属，T2 §2.2）或 `sources` 触发；`G→R/T→G` 往复合法（T2 §2.3），但必须是**新的、seq 递增的真实事件**（T2 I4）。
- **禁止**：在工具等待期间展示子进度条或"分析中 50%"。

### 6.3 信息暴露边界（T2 §7 的可操作化）

**可暴露**：工具/检索的**用户可理解名**（"联网搜索/获取网页/检索资料/运行代码/整理笔记"）。

**必须隐藏**：provider/模型名、调用轮次、token 计数、重试次数、退避时长、上下文摘要内部、Kernel 装配细节、`seq`/恢复机制、工具返回值原文。

> **决策价值判定**：某信息是否上屏，唯一判据是"用户能否据此做决策（继续等/取消/重试/改问）"。仅此即足够；不因"后端真实在跑"而增加任何暴露（T2 §7 原则）。

---

## 7. WAIT_FOR_INPUT（PIN）的暂停语义

> 完成标准 #8：不将用户思考时间表现为系统仍在处理。

- **暂停语义**：进入 `PIN` 后，系统处理反馈**暂停**。唯一允许的呈现是 E8 答题卡片；**无进度指示、无"正在处理…"、无系统活动类动效**。
- **计时豁免**：空闲计时器对 `PIN` 豁免（D6；现状 `failTurnIdle` 对 question 块直接放行，App.tsx L3088–3092）。用户思考时长不计入系统等待（T3 `SYS_WAIT` 排除 `[t_wi, t_ur]`；M6p）。
- **恢复**：`submit_user_reply` → 回到 `S`（由后续真实事件继续推进，T2 §2.1），并**重置 idle 计时器**（现状 `handleAnswerQuestion` 调 `armTurnIdleTimer`，App.tsx L3157–3162）。
- **离开**：仅 `submit_user_reply` 或用户 `cancel`（→CA）可离开；断线时等待重连恢复（T2 §8）。
- **体验要求**：卡片上应让用户清楚"这是**需要你回答**的暂停，不是系统在加载"，以显著区别于系统处理态。

---

## 8. 静默重试、断线、恢复、取消、超时、失败、完成的统一体验语义

> 完成标准 #9。所有异常语义遵循 P6（可解释、可行动、非破坏）。

| 场景 | P/传输语义 | 体验要求 | 禁止 |
|---|---|---|---|
| **静默重试**（S5，LLM 指数退避） | 仍在 `S`，零变化（T2 §4/§8） | 维持 E1+E2；重试不可见（合法隐藏） | "正在重试第 N 次/预计 Xs 后再试"等假进度（B4） |
| **断线**（G5） | 传输=`disconnected`（**只由传输层真实机制驱动**：心跳超时/WS 断开/重连耗尽，T2 §1.2/§2.4；**禁止由应用层静默时长推断** `disconnected`），P 状态保持（I8） | 出现 E7 连接提示（`connectError` 横幅），**不终结** P、不清空已见内容 | 把断线表现为 P 终态（如清空/报失败）；用静默时长冒充断线（§5.2 B7） |
| **恢复** | 重连成功 → `resume_from(lastSeq)` 续播去重（T2 §2.4） | 恢复原 P 状态反馈；已渲染内容保留；清除/减弱连接横幅 | 重放旧事件造成内容重复 |
| **取消**（CA） | 用户停止 / cancelled | 停止流式、**保留已渲染流式件**（T1 §12.7 不变量）；显示已取消 | 删除用户已见内容、或假装未取消 |
| **超时**（TO） | 前端 idle timer 触发（非 PIN），先到者胜（I7） | 非破坏性追加超时块，提示"可延长等待或重试"（现状文案，App.tsx L3098–3102） | 静默无限等待；超时与后端终态并存混乱 |
| **失败**（ER） | 终态 `error(failed, turn_terminal)` | 尾部错误块（非破坏），给出可行动的重试路径（`STREAM_FAIL_ERROR`"回复失败，请重试"语义） | 把失败伪装成"处理中"或成功 |
| **完成**（OK） | `done(completed)` | 停止流式、保留内容、清理活动指示（E5） | 终态后继续显示处理态 |

> **统一语义要点**：`ER/CA/TO` 三类终态反馈**结构一致**——都是"非破坏性尾部提示块 + 清理活动指示 + 可行动路径（重试/延长等待/确认已取消）"，只在文案与行动上区分。传输层（断线/恢复）始终**正交**于 P 终态，不混为一谈（T2 I8）。

---

## 9. 维持 / 升级 / 降级 / 终态反馈决策表（完成标准 #10）

> 触发条件是 **O 平面真实可观测事实**（决定性事件/文案级事件/传输事实/用户动作/空闲计时器，见 §2 P1）；**只有决定性事实可推进 P 状态**（文案级/传输事实只作用于文案与传输覆盖层）。动作是反馈元素的组合，全部由 T2 状态机驱动。

| 情形 | 触发（可观测事实） | 动作 | 禁止 |
|---|---|---|---|
| **维持** | 无新事件；或仅文案级事件（`thinking/progress/stage_start`） | 保持当前态反馈；文案可温和升级（A2） | 进度/阶段伪造 |
| **升级** | 决定性事件到达 | 按转换表升级反馈：`S→R/T`（E3 工具指示）、`S→G`（E4 流式内容为主）、`G→R/T`（多步新阶段，E3 短暂出现） | 由计时器触发的升级（D4） |
| **降级** | （无合法触发） | **不提供"降级"反馈路径**——活动态回退到更早文案属非法；`result`/空 `content` 的文案回退视为映射噪声，直接禁止（D5） | `result`/空 `content` 回退文案 |
| **进入终态** | `done/error/cancel/idle timer`（先到者胜，I1/I7） | 清理活动指示 → 输出对应终态反馈（E5/E6），终态后事件丢弃 | 终态后再显示处理态 |
| **传输变化** | 传输层**真实事实**：WS 客户端置位 `disconnected/reconnecting/connected`（T2 §1.2；**禁止由应用层静默时长推断** `disconnected`） | 叠加/解除 E7 传输提示，**不触碰** P 反馈（I8） | 把断线当终态处理；用静默时长冒充断线事实（§5.2 B7） |

---

## 10. 禁止使用的等待体验模式（完成标准 #11，明确清单）

| # | 禁止模式 | 说明 | 违反原则 |
|---|---|---|---|
| F1 | 虚假进度 | 无决定性事件却显示进度条/百分比 | P1；T2 I2/I3 |
| F2 | 伪阶段 | 无 `tool_call/sources` 却显示"正在检索/调用工具"；无 `content` 却显示"正在生成" | P1；T2 I4 |
| F3 | 伪 ETA | 任何"预计还需 Xs / 剩余时间"的呈现均**默认禁止**；完成 P95/P99 等时长校准（`C1–C4`）**不自动**赋予 ETA 合法性——仅当未来存在**独立且经验证的 ETA 预测契约**时方可另行评审准入 | P1；§3.3/§5.3 |
| F4 | 无意义阶段轮换 | 计时器驱动的 `S→R→T→G` 假循环 | P2；T2 I3（计时器只产 TO） |
| F5 | 动画掩盖无进展 | 用加快转圈/进度动画暗示"仍在推进"以掩盖无事件 | P1；T1 G2 |
| F6 | 暴露内部技术细节 | provider/模型名、重试次数、退避时长、token 数、工具参数 | P3；T2 §7 隐藏 |
| F7 | 用"思考中"冒充生成 | `thinking` 文案进入 G 语义或覆盖真实内容 | P4；T2 I2（thinking 不进 G） |
| F8 | 用户思考表现为系统处理 | `PIN` 中显示进度/处理文案/计数 | P5；T3 `SYS_WAIT` |
| F9 | 破坏性终态 | 失败/取消/超时时删除用户已见内容 | P6；T1 §11.5 |
| F10 | 超时与后端终态并存混淆 | 不按"先到者胜"处理 TO 与 OK/ER | P2；T2 I7 |

---

## 11. PASS / WARN / FAIL 验收框架（设计评审 + 后续 UI 实现）

> 两类验收规则：**静态审查项**（确定性结构规则，无需数据即可生效）与 **度量项**（依赖 T3 分布，`CALIBRATION_PENDING` 校准后生效）。

### 11.1 静态审查项（立即生效，设计评审必查）

对任何"等待交互设计 / UI 实现"，逐项检查：

| 检查项 | 判据 | 结果 |
|---|---|---|
| 同 tick 首反馈 | `submit` 同渲染 tick 出现 E1 | 是→PASS；否→FAIL |
| 首反馈本地性 | E1 仅为 `t_submit` 同 tick 本地即时反馈；**未将 E1 等同于 TTFD(M2)/TTFMR(M3)**（§4.1） | PASS/FAIL |
| 状态映射一致性 | 反馈状态与 T2 状态机一一对应，无新增 P 状态、无合并冲突 | PASS/FAIL |
| 事件驱动性 | 每个反馈升级可回溯到对应决定性事件（D4） | PASS/FAIL |
| 传输事实驱动 | 断线/重连提示均由传输层真实事实（心跳超时/WS 断开/重连耗尽）触发；**无由应用层静默时长推断断线**（§5.2 B7） | PASS/FAIL |
| 禁止模式扫描 | §10 F1–F10 无一出现 | 任一命中→FAIL |
| 非破坏性 | `ER/CA/TO` 均为尾部追加、保留已见内容 | PASS/FAIL |
| PIN 豁免 | `PIN` 无处理指示、豁免 idle 超时 | PASS/FAIL |
| 内容-状态分离 | `G` 中流式内容不被状态文案覆盖 | PASS/FAIL |

### 11.2 度量项（依赖 T3，校准后生效）

| 检查项 | 判据（校准后） | 结果（校准前） |
|---|---|---|
| 档位边界合理 | `M2/M3` 的 P50/P95 落在"普通"档 | `CALIBRATION_PENDING`（不判 FAIL） |
| 长静默升级有效 | `M5(P95)` 处有对应升级反馈 | `CALIBRATION_PENDING` |
| 超时安抚有效 | `M8-t2` 处有"仍在等待"提示 | `CALIBRATION_PENDING` |
| 断线可见化有效 | `M7a` 处叠加传输提示 | `CALIBRATION_PENDING` |
| 总体体验回归 | `M4s/M2/M3/M5` 分位漂移在阈值内（T3 §10 漂移判定） | baseline 建立后生效 |

> **判定总则**（对齐 T3 §10）：静态项任一 FAIL → 整体 FAIL（设计阻断）；静态项全 PASS 且度量项无"校准前误判" → WARN（待真实数据校准确认）；静态项全 PASS 且（校准后）度量项全 PASS → 整体 PASS。校准前的度量项**不得**被判 FAIL（避免凭空设定）。

### 11.3 评审产物

- 设计评审输出：逐项检查表 + 命中/未命中清单 + 整体 PASS/WARN/FAIL 结论。
- 后续 UI 实现验收：同一检查表在实现后复跑；度量项以 T3 baseline 归档（T3 §9）。

---

## 12. 覆盖 T1 G1–G8 / S1–S6（完成标准 #13 前半，自检表）

| 项 | 本规范处理位置 |
|---|---|
| G1 / S1 首 token 静默 | §4.1/§4.2、§5（静默维持 + 兜底）；T3 M3 基线 |
| G2 / S5 静默重试不可见 | §8 静默重试、§5.2 B4、F1/F5；idle timer 兜底 |
| G3 / S2 摘要被过滤 | §4.2（摘要并入 S 合法静默）、§5 允许 A1/A2 |
| G4 / S4 工具无子进度 | §6.1/§6.2（工具名粒度、禁子进度）、§1.2 `R/T` |
| G5 断线不复位 | §8 断线/恢复、E7、§9 传输变化（I8 正交） |
| G6 / S3 冷启动 | §4.2（并入 S）、§5（长静默处理含冷启动档） |
| G7 标题生成 | §4.6（OK 后非阻塞装饰）、T1 G7（经 T2 §5 归为非决定性） |
| G8 重复提交 | §2 P2/P7 + T2 I5/I10（submitLock 守卫为前置事实，非本规范新增） |
| 成功完成 | §4.6 E5、§8 完成 |
| 失败 / 取消 / 超时 | §8 统一终态语义、§9 进入终态、E6 |
| 等待用户输入 | §7 PIN、§1.2 `PIN`、M6p |

=> **G1–G8、S1–S6 全部覆盖，无遗漏。**

---

## 13. 与 T2 / T3 的一致性对照（完成标准 #13 后半）

| T2/T3 冻结语义 | 本规范引用 | 一致性 |
|---|---|---|
| 状态集合与转换表（T2 §2.1） | §1.2/§4.3/§9 | 原样沿用，不新增/修改 |
| 决定性/文案级白名单（T2 §5/§11.4） | §1.1/§2 P1/§4 | 反馈元素只由 O 平面真实可观测事实驱动（§2 P1 五类输入）；**P 推进仅由决定性事实**（决定性事件∪决策性用户动作∪空闲计时器） |
| 不变量 I1–I11（T2 §9） | §2 P2、§8、§9、§10 F10 | 逐条遵守（终态闩锁、真因推进、禁假进度、seq、单活跃等） |
| 三平面/三条时间轴（T2 §0/§11.1） | §0、§3 | 感知体验只由客户端可观测事实驱动 |
| `G` 只由 `content(非空)` 进入（T2 I2） | §4.4/§4.5、F7 | 一致 |
| PIN 豁免、用户思考不属系统等待（T2 §8；T3 §4/M6p） | §7、F8 | 一致 |
| M1–M8 / `SYS_WAIT` / 场景桶 / 分位（T3 §3–§7） | §3（分层引用 M2/M3/M5/M4s/M7a/M8-t2） | 只引用，不修改 |
| `CALIBRATION_PENDING` 不生效为告警（T3 §10） | §3.3 C1–C4、§11.2 | 一致 |

=> **与 T2/T3 完全一致，无新增语义矛盾。**

---

## 14. T5 系统映射与后续 UI 实现的输入边界（完成标准 #14）

本规范是 **T5（系统映射）与后续 UI 实现**的**设计输入**，交付边界如下：

**输出给 T5/UI 的契约**：
1. **反馈元素↔P 状态对照表**（§1.2）：每个 P 状态允许/禁止的反馈元素。
2. **确定性结构规则 D1–D7**（§3.2）：T5 系统映射必须实现的硬约束。
3. **各场景体验语义**（§4–§8）：首次反馈/持续/阶段切换/首结果/完成/PIN/异常的统一语义。
4. **维持/升级/降级/终态决策表**（§9）与**禁止模式清单**（§10）。
5. **验收检查表**（§11）：T5 与 UI 实现后必须复跑。

**明确不在此阶段交付**（T5 及以后）：
- 事件→反馈元素的**具体实现映射代码**（T5 系统映射职责）。
- 任何 `CALIBRATION_PENDING` 阈值的**落地数值**（须待 T3 真实采集后回填）。
- 任何生产 UI / 交互代码改动。

> **边界规则**：T5/UI 实现不得绕过 §11 验收框架；任何改变等待语义的改动必须先通过静态审查项，度量项以 T3 baseline 复测。本阶段**不进入 T5**（见 §15）。

---

## 15. 修正验收与 FROZEN BASELINE 声明

> 完成标准逐项核验，全部通过后正式冻结本 T4。

| 完成标准 | 落实 |
|---|---|
| ① 所有核心感知状态的用户反馈原则 | §1.2 状态↔元素对照表 + §2 原则 P1–P7 + §4–§8 语义 |
| ② 等待时长反馈分层，区分确定规则与待校准规则 | §3.1 分层 + §3.2 D1–D7（确定）+ §3.3 C1–C4（CALIBRATION_PENDING） |
| ③ 首次反馈/持续/阶段切换/首个有效结果/最终完成 | §4.1–§4.6 |
| ④ Max Silent Gap 体验处理原则 | §5.3（度量归 T3；兜底、升级时机校准、禁动画掩盖） |
| ⑤ `S` 长静默允许/禁止表现 | §5.1 A1–A4 / §5.2 B1–B7 |
| ⑥ Retrieval/Tool/多步 Agent 粒度、切换、暴露边界 | §6.1–§6.3 |
| ⑦ `G` 流式内容与状态反馈关系 | §4.5 + P4 + F7 |
| ⑧ WAIT_FOR_INPUT 暂停语义（不表现为系统处理） | §7 + F8 + T3 M6p 对齐 |
| ⑨ 静默重试/断线/恢复/取消/超时/失败/完成统一语义 | §8 统一终态语义 + I8 正交 |
| ⑩ 维持/升级/降级/终态决策 | §9 决策表（降级=禁止） |
| ⑪ 禁止模式清单 | §10 F1–F10 |
| ⑫ PASS/WARN/FAIL 验收规则 | §11（静态项立即生效；度量项校准后生效，校准前不判 FAIL） |
| ⑬ 覆盖 G1–G8/S1–S6 且与 T2/T3 一致 | §12 自检无遗漏；§13 对照无矛盾 |
| ⑭ 输出本文档并明确 T5 输入边界 | 本 §14 |
| ⑮ 全部通过后标记 FROZEN BASELINE | 见下 |

### 15.1 语义一致性修正记录（2026-08-20，5 项已确认问题）

> 本次修正以**现有 T4 为基线**，只做必要修正、避免重构；T1/T2/T3 冻结语义未做任何修改。以下 5 项问题逐项确认解决：

| # | 已确认问题 | 修正内容 | 落点 |
|---|---|---|---|
| 1 | "事实驱动"输入边界未统一（缺文案级/传输事实；未明确推进权） | 统一为 O 平面五类输入（决定性事件/文案级事件/传输事实/用户动作/空闲计时器），明确**只有决定性事实推进 P 状态** | §2 P1；§9 决策表头；§13 对照 |
| 2 | E1 被等同于 TTFD/M2（"对应 T3 M2 的体验基线"） | E1 明确定义为 `t_submit` 同 tick **本地即时反馈**，非决定性事件、**不进 M1/M2/M3 口径**，禁止与 TTFD(M2)/TTFMR(M3) 混同 | §1.1 E1；§3.2 D1；§4.1；§11.1 新增"首反馈本地性" |
| 3 | F3 表述暗示 C2/C3 校准后 ETA 可合法 | ETA **默认禁止**；P95/P99 时长校准（C1–C4）**不自动**赋予 ETA 合法权，仅当未来存在**独立且经验证的 ETA 预测契约**时方可评审 | §3.3 "ETA 准入红线"；§5.3 #5；§10 F3 |
| 4 | 断线提示可能由应用层静默时长推断 | `disconnected` 只能由传输层真实事实（心跳超时/WS 断开/重连耗尽）置位；E7/§8/§9 明确禁止静默推断；新增 §5.2 B7 | §1.1 E7；§3.3 C4；§5.2 B7；§5.3 #6；§8 断线；§9 传输变化；§11.1 新增"传输事实驱动" |
| 5 | 错误交叉引用（T3 G4→T1 G4、B2 F4→F3、B4 F8→F6、B5 F6→F5、G7 引用等） | 全部修正并经全文编号/交叉引用/术语扫描 | §1.2、§4.6、§5.2、§12、§13 |

> 一致性复核：修正后全文状态↔元素对照、D1–D7、PIN、R/T/G、异常语义与 PASS/WARN/FAIL 框架保持不变；与 T2/T3 冻结语义重新逐项核对，无新增矛盾；G1–G8 / S1–S6 覆盖完整（§12）。

**结论**：上述 15 项完成标准全部通过，且 §15.1 所列 5 项修正问题全部解决。**T4 per `WAITING_EXPERIENCE_DESIGN_SPEC_V1.md` 重新标记为 `FROZEN BASELINE`**（`FROZEN_BASELINE = "t4-waiting-experience-design-spec-v1"`）。确定性结构规则（D1–D7、禁止模式、验收检查表）即刻生效；依赖真实数据的时长阈值标注 `CALIBRATION_PENDING`（不生效为任何 UI 逻辑），待 T3 采集后回填。**不实施生产 UI / 不改代码 / 不进入 T5 实现**，仅交付冻结的设计规范契约。

——
