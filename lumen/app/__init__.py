"""App layer — startup / CLI / API glue owned by ``lumen``.

Home of the runtime launcher, CLI/server mode switch, banner, SPA server and
system-setup helpers.  Contracts and modes live under ``lumen/runtime`` /
``lumen/modes``; this package wires them together for entry points.
"""
