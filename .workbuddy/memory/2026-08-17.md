# 2026-08-17 工作日志

## Lumen 插件化重构前裁剪：Batch 5/6/10/11/12 并行执行

依据 `docs/reports/lumen-pre-plugin-pruning-plan.md`，用 5 个独立 worktree 并行完成裁剪，**未 merge**（按用户指示）。

### 基线
- `prune/baseline` @ fb16df1e（含批次 0-4 裁剪状态）
- 干净基线全量 pytest：2423 passed, 7 skipped, EXIT=0
- ⚠️ 主仓库 `/Users/xike/Documents/Docs/Lumen` 工作区有**未提交半成品**：batch4 visualize 删除进行中 + `api/main.py` 中 book 路由移除时把 `)    unified_ws,` 改坏成语法错误（行 286）。**未被本次裁剪触碰**，需要后续人工处理或确认。

### 各 Batch 结果（worktree 分支，未 merge）
| Batch | 分支 | commit | 全量 pytest | 说明 |
|---|---|---|---|---|
| 5 github | prune/batch5-github | 19fa8997 | 2413 passed | 删 GithubTool/github_query/孤儿 hints yaml + 注册/挂载/测试/文档 |
| 6 exec | prune/batch6-exec | c56ad8f7 | 2421 passed | 删 ExecTool 及注册/gate/agentic_pipeline 分支/path_service 判定/文案；保留 code_execution + services/sandbox + read/write_file；顺带把 office skill SKILL.md 中 exec→code_execution 文案更新 |
| 10 voice | prune/batch10-voice | 6cedd719 | 2399 passed | 删 services/voice + voice 路由 + 前端 voice.ts/App.tsx 朗读链路/SettingsModal/settings.ts/globals.css + 前端测试断言；前端 build 通过；Playwright 冒烟 2 个失败经 git stash 复跑确认为**预存失败**（batch1/3 裁剪后前端测试未同步，与 voice 无关） |
| 11 cron | prune/batch11-cron | 7bb9f6bf | 2401 passed | 删 services/cron + CronTool + 注册 + agentic_pipeline cron 分支 + api lifespan + deeptutor_cli/chat.py 中 cron 挂载（计划外发现）+ croniter 依赖（pyproject + requirements/server.txt） |
| 12 office | prune/batch12-office | f704cd7f | 2423 passed | 删 skills/builtin/{docx,pdf,pptx,xlsx,skill-creator}/SKILL.md + README 章节 + 前端 pages.tsx mock（github-actions/ngs-analysis）；不动 services/parsing 与 Skill Framework |

所有 batch：`validate_tool_consistency()` 通过；红线目录（learning/teaching_core/mastery_path）零改动；工作区干净。

### 环境备忘
- 测试环境：`/opt/miniconda3/bin/python3 -m pytest`（conda python 3.13.2 + pytest 8.4.2），worktree 根目录运行（pythonpath="."）
- 前端 build：`cd frontend && npm run build`（tsc -b && vite build）；沙箱会拦 vite 清空 dist，需 `env -u NODE_OPTIONS`
- Playwright：`cd frontend && npx playwright test <spec>`；vite dev server 偶发挂起需手动清理
- validate_tool_consistency 位于 deeptutor/api/main.py，`python3 -c "from deeptutor.api.main import validate_tool_consistency; validate_tool_consistency()"`
- 遗留：b5 报告提示 Batch 7 处理 Copilot provider 时需评估 skill/service.py 与 skill-creator 的 GITHUB_TOKEN 引用（skill-creator 已在 b12 删除）

### 待办
- 主仓库工作区坏语法 + 未提交半成品需处理
- Batch 1/3 裁剪后前端测试（hover-and-menus / settings-general）预存失败待后续批次同步
- 各 batch 分支未 merge，等用户决定
